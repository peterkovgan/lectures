package com.monster;

public enum Mode{
    BOTH,      //Mode used to start both in single VM
    RESPONDER, //Mode used to start server in separate VM
    INITIATOR  //Mode used to start client in separate VM
}
